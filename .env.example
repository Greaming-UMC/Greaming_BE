# ===================================================================
# 환경변수 설정 가이드
# ===================================================================
# 1. 이 파일을 복사하여 프로젝트 루트에 '.env' 파일을 생성하세요.
#    cp .env.example .env
#
# 2. 각 환경변수 값을 본인의 환경에 맞게 설정하세요.
#
# 3. '.env' 파일은 .gitignore에 등록되어 Git에 올라가지 않습니다.
# ===================================================================

# ===================================================================
# 🔧 서버 설정
# ===================================================================
# 서버 포트 (로컬: 8080, 개발: 8081, 운영: 8080)
SERVER_PORT=8080

# ===================================================================
# 💾 데이터베이스 설정
# ===================================================================
# MySQL 연결 URL
# 로컬: jdbc:mysql://localhost:3306/greaming_local?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
# 개발: jdbc:mysql://dev-db-server:3306/greaming_dev?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
# 운영: jdbc:mysql://prod-db-server:3306/greaming_prod?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/greaming_local?serverTimezone=Asia/Seoul&characterEncoding=UTF-8

# MySQL 계정 정보
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=your_password_here

# ===================================================================
# 🔍 JPA 설정
# ===================================================================
# Hibernate DDL 자동 생성 전략 (create, create-drop, update, validate, none)
# 로컬/개발: update 권장
# 운영: validate 권장 (서비스 안정화 후)
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# SQL 쿼리 로그 출력 여부
# 로컬/개발: true (디버깅 편의성)
# 운영: false (성능 최적화)
SPRING_JPA_SHOW_SQL=true

# ===================================================================
# 🔐 JWT 설정
# ===================================================================
# JWT 비밀키 (최소 32자 이상, 256-bit 이상 권장)
# ⚠️ 운영 환경에서는 반드시 강력한 랜덤 문자열로 변경하세요!
# 생성 방법: openssl rand -base64 64
JWT_SECRET=your-256-bit-secret-key-here-must-be-at-least-32-characters-long-for-security

# ===================================================================
# 🔑 OAuth2 소셜 로그인 설정
# ===================================================================

# ----- Google OAuth2 -----
# Google Cloud Console에서 발급받은 Client ID
# https://console.cloud.google.com/
OAUTH2_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google Cloud Console에서 발급받은 Client Secret
OAUTH2_GOOGLE_CLIENT_SECRET=your-google-client-secret

# ----- Kakao OAuth2 -----
# Kakao Developers에서 발급받은 REST API Key
# https://developers.kakao.com/
OAUTH2_KAKAO_CLIENT_ID=your-kakao-rest-api-key

# Kakao Developers에서 발급받은 Client Secret
OAUTH2_KAKAO_CLIENT_SECRET=your-kakao-client-secret

# ----- OAuth2 리다이렉트 URI -----
# 프론트엔드 OAuth 콜백 URL
# 로컬: http://localhost:3000/oauth/callback
# 개발: https://dev.yourdomain.com/oauth/callback
# 운영: https://yourdomain.com/oauth/callback
OAUTH2_REDIRECT_URI=http://localhost:3000/oauth/callback

# ===================================================================
# ☁️ AWS S3 설정
# ===================================================================
# AWS IAM에서 발급받은 Access Key
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS IAM에서 발급받은 Secret Access Key
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# ===================================================================
# 🚀 사용 방법
# ===================================================================

# --- IntelliJ IDEA ---
# 1. EnvFile 플러그인 설치
#    Settings (⌘,) > Plugins > "EnvFile" 검색 > Install
#
# 2. Run/Debug Configuration 설정
#    Run > Edit Configurations... > EnvFile 탭
#    > "Enable EnvFile" 체크 > .env 파일 추가
#
# 3. 실행

# --- 터미널 (macOS/Linux) ---
# export $(cat .env | xargs) && ./gradlew bootRun

# --- 터미널 (Windows PowerShell) ---
# Get-Content .env | ForEach-Object { $var = $_.Split('='); [Environment]::SetEnvironmentVariable($var[0], $var[1]) }
# .\gradlew.bat bootRun

# --- Docker Compose ---
# services:
#   app:
#     env_file: .env

# ===================================================================
# 💡 환경별 프로필 전환
# ===================================================================
# Spring Profile을 통해 local/dev/prod 환경을 전환할 수 있습니다.
# 기본값은 'local'입니다.

# 로컬 환경 (기본값)
# ./gradlew bootRun

# 개발 환경
# export SPRING_PROFILES_ACTIVE=dev
# ./gradlew bootRun

# 운영 환경
# export SPRING_PROFILES_ACTIVE=prod
# ./gradlew bootRun
